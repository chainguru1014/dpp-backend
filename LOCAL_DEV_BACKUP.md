# Local Dev Backup (Backend)

## Setup

1. Copy `env.sample` to your local env file.
2. Install dependencies: `npm install`

## Run

- Development: `npm run dev`
- Production-style: `npm run build && npm start`

Default local API port is `5052` (or `PORT` if set by host).
